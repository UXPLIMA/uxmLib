package com.uxplima.uxmlib.nametag;

/** How multi-line nametag text is justified, mirroring vanilla's {@code Display.TextDisplay.Align}. */
public enum Alignment {

    /** Lines are centred. */
    CENTER,

    /** Lines are left-justified. */
    LEFT,

    /** Lines are right-justified. */
    RIGHT
}
